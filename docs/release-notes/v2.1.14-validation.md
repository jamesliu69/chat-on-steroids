# 2.1.14 release validation

The release is a reviewed working-tree snapshot based on public main. Private development history, local worklogs and runtime evidence are excluded. Original contributor credit already present on main is retained. The optional pet includes its runtime assets and accepted artwork sources.

Preparation checks passed: synchronized app/lockfile/extension versions, staged public-history privacy, added-file credential/path scan, third-party notices and native-source inventory, current tunnel pin, TypeScript and production build. Real isolated Chromium browser-control acceptance passed. Full production Electron pet acceptance passed after preventing host-focus background throttling in the isolated test window; production visibility and background policy are unchanged. The bounded full suite passed 5,058 tests (44 intentionally skipped), followed by all six isolated shutdown tests. The first unrestricted run had two native Windows capture no-frame failures; both passed in the independent complete desktop suite and the full rerun. The final countdown visibility change passed 660 targeted bridge, delivery and renderer cases plus TypeScript. Hosted cross-platform checks are required at the final head before merge; all six native release targets, packaged smoke checks and assembled checksums are required before publication.

Local installation and signed-in provider acceptance are separate from these release gates and are not claimed.
